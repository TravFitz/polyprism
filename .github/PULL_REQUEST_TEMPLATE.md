## What this PR does

<!-- One or two sentences. -->

## Why

<!-- The motivation — bug fix, new feature, perf, refactor, etc. Link to
     the issue this resolves if there is one. -->

## How to verify

<!-- Steps a reviewer can take to confirm the change works. If you added
     tests that exercise it, just point to the test file. -->

## Checklist

- [ ] `pnpm run ci` passes locally
- [ ] `pnpm typecheck` passes locally
- [ ] `pnpm test` passes locally
- [ ] `pnpm build` passes locally
- [ ] Added a `.changeset/*.md` entry if the change is user-visible
- [ ] Updated relevant docs (README / per-package README / examples) if behaviour or API changed
- [ ] The change does not introduce a new runtime dependency on any published `@omniprism/*` package
