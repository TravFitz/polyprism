node_modules/
dist/
coverage/
.turbo/
.cache/

.DS_Store
*.log
*.tsbuildinfo

.idea/
*.iml
.vscode/

# AI agent / pairing tooling — keep private project instructions out of the
# public repo. Add the corresponding file to your team's onboarding doc if
# you want others to benefit from the same setup.
CLAUDE.md
.claude/
AGENTS.md
.cursorrules
.cursor/
.aider*
.continue/
.windsurfrules

.env
.env.local
.env.*.local

.pnpm-store/

# Local-only helper scripts — useful tools that aren't load-bearing for
# the published packages and don't belong in the public repo. Living
# inside `scripts/` so they sit next to `guard-release.mjs` where you'd
# look for them, but tracked locally only.
scripts/npm-download-stats.mjs
