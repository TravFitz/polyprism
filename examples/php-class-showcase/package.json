{
  "name": "polyprism-example-php-class-showcase",
  "version": "0.0.0",
  "private": true,
  "description": "Showcase example for php-class — exercises every PHP scalar mapping, enums with defaults, relations (1-many + self-reference), every default-shape (literal, enum case, now(), cuid()), and the annotation set (@hide, @deprecated, @name, @type, @json fallback).",
  "type": "module",
  "scripts": {
    "generate": "prisma generate",
    "clean": "rm -rf generated"
  },
  "dependencies": {
    "@polyprism/php-class": "workspace:*",
    "@prisma/client": "^6.19.3"
  },
  "devDependencies": {
    "prisma": "^6.19.3"
  }
}
